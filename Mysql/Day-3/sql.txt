SQL : structured query language
structured means syntax
SQL is also called SEQUEL
SEQUEL menas SE menas structured ,QUE means query and L means language
it is not a programming language and it is a declarative language
declarative language means we will tell here what we need insted of how to do
sql operations are CRUD
C means create
R means retrive
U means update
D means Delete
** we use keyword to write in uppercase
What is create?
1.Table
2.store records : in this we use operations insert and command(clause)
3.the command to CREATE TABLE tablename(
    columnname1 datatype,
    columnname2 datatype
);

