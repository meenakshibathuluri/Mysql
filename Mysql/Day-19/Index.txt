//NOT NULL : ensure that column data should not have null values
//unique : ensure all the values inside the column should be different 

CREATE TABLE mysql_learning.persons(
    employee_id INT NOT NULL,
    name VARCHAR(250) NOT NULL,
    manager_id INT,
    salary INT NOT NULL,
    organization name VARCHAR(250) DEFAULT 'Accenture'
);

SELECT * FROM mysql_learning.persons;

INSERT INTO mysql_learning.persons(
    employee_id,
    name,
    manager_id,
    salary,
) values 
(1,"MEENA",4,9647);